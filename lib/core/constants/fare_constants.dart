class FareConstants {
  FareConstants._();

  // Starting point for the auto-computed fare on the Pricing screen — the
  // driver can edit every field before publishing.
  static const double ratePerKm = 8.0;
  static const double detourRateMultiplier = 0.5;
}
